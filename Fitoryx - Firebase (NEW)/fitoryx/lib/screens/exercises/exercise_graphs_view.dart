import 'package:flutter/material.dart';

class ExerciseGraphsView extends StatelessWidget {
  const ExerciseGraphsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Graphs');
  }
}
